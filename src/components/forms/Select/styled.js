import { flex, layout, position, space } from "styled-system";
import { Select as BaseSelect } from "@ui-kitten/components";
import styled from "styled-components/native";

export const Select = styled(BaseSelect)`
  ${space}
  ${layout}
  ${flex}
  ${position}
`;
