import styled from "styled-components/native";
import { space, border, layout, position, flexbox, color } from "styled-system";

export const IconButton = styled.TouchableOpacity`
  ${space}
  ${border}
  ${layout}
  ${position}
  ${flexbox}
  ${color}
`;
